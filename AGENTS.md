# Roguelike 项目开发强制规范

## 数据驱动

- 项目配置数据的唯一源头是数据表，数据表统一通过 [Luban](https://www.datable.cn/docs/intro) 转换。
- 游戏规则、数值、概率、权重、时间、距离、数量、资源键、文本及功能参数不得硬编码在 C#、Prefab、Scene 或 ScriptableObject 中。
- 运行时代码只能通过统一配置入口读取 Luban 生成的数据；禁止在代码中复制一套默认值或兜底配置。
- Luban 的表结构、生成配置、生成代码和运行时数据属于功能交付的一部分，必须可在新机器复现。
- 纯技术常量若确实不属于策划配置，必须在方案讨论阶段明确边界后才能写入代码。
- 当前已有的 ScriptableObject 配置及硬编码属于待迁移遗留，不得作为新功能范例继续扩展。

## 功能开发流程

每个正式功能必须按顺序执行，前一步未确认不得进入下一步：

1. **讨论实现方案**：明确功能边界、数据流、表结构、运行时架构、兼容影响与验收标准。
2. **完善数据配置表**：完成表定义与数据，运行 Luban 生成并校验生成物。
3. **代码实现**：只消费生成配置，不新增业务硬编码。
4. **测试验收**：按已确认的验收标准执行测试并记录结果。

测试工具、测试用例和纯测试任务可跳过上述功能流程，但不得向正式运行逻辑引入硬编码配置。

## 完成标准

- 方案、表结构和验收标准已确认。
- Luban 生成流程成功，生成结果可由新机器复现。
- 代码中不存在该功能的业务硬编码或与数据表重复的默认配置。
- 编译、自动化测试和人工验收均通过。

