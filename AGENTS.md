# Roguelike 项目开发强制规范

## 工作目录与验收顺序

- 所有开发、同步后的测试和验收统一在原项目目录进行；未经用户明确要求，不创建或使用额外 Git worktree。
- 先完成 Unity Editor 自动化与交互验证，再由用户人工验收；Win64 包延后至全部功能完成且用户明确要求后才构建，不作为每轮验收的下一步。
- 同步其他目录修改时先核对两边改动并备份，保留原项目全部已有未提交改动，不覆盖 Scene/Prefab。

## 数据驱动

- 项目配置数据的唯一源头是数据表，数据表统一通过 [Luban](https://www.datable.cn/docs/intro) 转换。
- 游戏规则、数值、概率、权重、时间、距离、数量、资源键、文本及功能参数不得硬编码在 C#、Prefab、Scene 或 ScriptableObject 中。
- 运行时代码只能通过统一配置入口读取 Luban 生成的数据；禁止在代码中复制一套默认值或兜底配置。
- Luban 的表结构、生成配置、生成代码和运行时数据属于功能交付的一部分，必须可在新机器复现。
- 纯技术常量若确实不属于策划配置，必须在方案讨论阶段明确边界后才能写入代码。
- 当前已有的 ScriptableObject 配置及硬编码属于待迁移遗留，不得作为新功能范例继续扩展。

## 功能开发流程

每个正式功能必须按顺序执行，前一步未确认不得进入下一步：

1. **讨论实现方案**：明确功能边界、数据流、表结构、运行时架构、兼容影响与验收标准。
2. **完善数据配置表**：完成表定义与数据，运行 Luban 生成并校验生成物。
3. **代码实现**：只消费生成配置，不新增业务硬编码。
4. **测试验收**：按已确认的验收标准执行测试并记录结果。

测试工具、测试用例和纯测试任务可跳过上述功能流程，但不得向正式运行逻辑引入硬编码配置。

## 完成标准

- 方案、表结构和验收标准已确认。
- Luban 生成流程成功，生成结果可由新机器复现。
- 代码中不存在该功能的业务硬编码或与数据表重复的默认配置。
- 编译、自动化测试和人工验收均通过。

## 代码注释规范

- 所有手写函数都必须使用 XML 文档注释，至少说明函数职责、调用时机和关键处理流程，禁止只复述函数名。
- 存在参数、返回值或可预期异常时，必须分别补充 `<param>`、`<returns>`、`<exception>`。
- 函数会修改全局状态、发布事件、读写存档、加载资源、创建或销毁 Unity 对象时，必须在 `<remarks>` 中说明副作用及生命周期要求。
- 消费配置数据时，注释必须写明数据来源的 Luban 表及关键字段，禁止在注释中复制可能变化的具体数值。
- Unity 生命周期函数必须说明由 Unity 在何时调用，以及该函数依赖的初始化前置条件。
- 复杂分支、性能敏感路径和临时兼容逻辑必须解释“为什么这样处理”，禁止使用无信息量的逐行翻译式注释。
- 修改函数逻辑时必须同步更新注释；过期注释视为缺陷，不允许保留。
- Luban 自动生成代码、Unity/第三方包源码不手工补注释；需要说明时在调用方或项目文档中记录，避免重新生成或升级时丢失。

## Git 提交规范

- 每次提交必须保持单一目的，源数据、Luban 生成代码及对应 bytes 必须在同一提交中完整出现。
- 提交标题使用 `<type>(<scope>): <中文摘要>`，例如 `feat(config): 接入角色基础配置`、`fix(stage): 修复关卡引用校验`。
- 提交正文不得省略，必须包含以下内容：
  1. `背景`：为什么进行本次修改。
  2. `变更内容`：按模块列出代码、资源和配置调整。
  3. `数据表与生成物`：列出修改的工作表、字段、ID 范围及重新生成的代码和 bytes；没有数据表修改时明确写“无”。
  4. `验证`：列出实际执行的命令、测试范围及结果，禁止只写“已测试”。
  5. `影响与风险`：说明兼容性、存档、资源、性能及回滚影响；没有已知风险时明确写“未发现”。
  6. `未包含`：列出本次刻意不处理的遗留项，避免误判为已经完成。
- 提交前必须检查 `git status` 和完整 diff，只提交本任务文件；用户未授权的场景、Prefab、配置及本地文件不得混入。
- 提交内容禁止包含账号、Token、联系方式、绝对本机路径或其他敏感信息。
